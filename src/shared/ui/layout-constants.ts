// Gutter horizontal mínimo que todo card/painel de página deve respeitar.
export const PAGE_GUTTER_X = 48;

// Border-radius padrão do sistema — espelha o token 'md' do tema Mantine (0.5rem = 8px).
// Qualquer raio de borda inline no app deve usar esta constante em vez de um número solto,
// para o visual não desalinhar do tema conforme o design evolui.
export const RADIUS_MD = 8;

// Altura da faixa de destaque (accent bar) no topo do header.
export const HEADER_ACCENT_HEIGHT = 15;
